CREATE TABLE form (form_id INTEGER PRIMARY KEY, created TEXT NOT NULL, id TEXT KEY NOT NULL, name TEXT KEY NOT NULL, datakey TEXT NOT NULL, data TEXT NOT NULL, status INTEGER KEY NOT NULL DEFAULT 0);
CREATE TABLE form_alternative (form_id INTEGER PRIMARY KEY, created TEXT NOT NULL, id TEXT KEY NOT NULL, name TEXT KEY NOT NULL, datakey TEXT NOT NULL, data TEXT NOT NULL, status INTEGER KEY NOT NULL DEFAULT 0);